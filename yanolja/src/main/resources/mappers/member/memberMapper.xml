<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.yanolja.member.MemberMapper">
	<resultMap type="com.care.yanolja.member.MemberDTO" id="memberDto">	
		<id column="user_id" property="userId" />
		<result column="user_pw" property="userPw" />
		<result column="user_mobile" property="userMobile" />
		<result column="user_dob" property="userDob" />
		<result column="user_email" property="userEmail" />
		<result column="user_name" property="userName" />
		<result column="user_snsC" property="userSnsC" />
	</resultMap>
	
	<select id="loginProc" resultMap="memberDto">
		SELECT * FROM MEMBER WHERE user_id=#{userId}
	</select>

	<insert id="registerProc"
		parameterType="com.care.yanolja.member.MemberDTO">
		INSERT INTO MEMBER (user_id, user_pw, user_mobile, user_name, user_email) 
        VALUES (#{userId}, #{userPw}, #{userMobile}, #{userName}, #{userEmail})
	</insert>

	<select id="count">
		<choose>
			<when test="select == 'id'">
				SELECT count(id) FROM MEMBER WHERE id like
				'%'||#{search}||'%'
			</when>
			<when test="select == 'mobile'">
				SELECT count(id) FROM MEMBER WHERE mobile like
				'%'||#{search}||'%'
			</when>
			<otherwise>
				SELECT count(id) FROM MEMBER
			</otherwise>
		</choose>
	</select>

	<select id="memberInfo" resultMap="memberDto">
		<choose>
			<when test="select == 'id'">
				SELECT AAA.* FROM
				(SELECT rownum as rn, id, pw,
				user_name, address, mobile
				FROM MEMBER WHERE id like
				'%'||#{search}||'%')AAA
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'mobile'">
				SELECT AAA.* FROM
				(SELECT rownum as rn, id, pw,
				user_name, address, mobile
				FROM MEMBER WHERE mobile like
				'%'||#{search}||'%')AAA
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<otherwise>
				SELECT AAA.* FROM
				(SELECT rownum as rn, id, pw, user_name,
				address, mobile
				FROM MEMBER )AAA
				WHERE AAA.rn BETWEEN #{begin}
				AND #{end}
			</otherwise>
		</choose>
	</select>

	<update id="updateProc"
		parameterType="com.care.yanolja.member.MemberDTO">
		UPDATE MEMBER
		SET pw=#{pw}, user_name=#{userName},
		address=#{address}, mobile=#{mobile}
		WHERE id=#{id}
	</update>

	<delete id="delete">
		DELETE FROM MEMBER WHERE id=#{id}
	</delete>
</mapper>


